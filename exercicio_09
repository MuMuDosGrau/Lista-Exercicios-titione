quantidade_numeros = int(input('insira a quantidade de numeros da lista: '))
lista_numeros = []

for i in range(quantidade_numeros):
    lista_numeros.append(int(input('Insira um numero inteiro: ')))

print(f'O maior numero da lista é: {max(lista_numeros)}')
